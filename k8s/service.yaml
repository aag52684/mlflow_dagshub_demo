apiVersion: v1
kind: Service
metadata:
  name: first-experiment-service
spec:
  selector:
    app: first-experiment-model
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort